---
plan: 10
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - .claude-plugin/plugin.json
  - CHANGELOG.md
autonomous: true
requirements: [R1, R3, R4]
---

<objective>
执行最终集成测试和发布准备：端到端飞轮测试、hardcoded path 扫描、更新 plugin.json 版本、完善 CHANGELOG，打 v0.1.0 tag。
</objective>

<context>
Origin: docs/plans/2026-04-02-001-feat-revolve-open-source-plugin-plan.md
Unit: 10 — Polish & Publish

所有 Unit 完成后的收尾：
1. 端到端飞轮测试：运行 /revolve-setup → /research-pipeline → /evolve-claude-md 完整流程
2. Hardcoded path 扫描：grep 所有 skill 文件确认无硬编码路径
3. 更新 plugin.json 确认 version=0.1.0
4. 完善 CHANGELOG.md 的 [0.1.0] 条目
5. 打 v0.1.0 git tag

Patterns to follow:
- 扫描 hardcoded path 的关键词：~/Knowledge Base、/Users/、hardcoded email/username
- plugin.json version 字段在发布前确认
- CHANGELOG 使用 Keep a Changelog 格式
- tag 格式：v0.1.0
</context>

<tasks>
<task type="auto">
  <name>Task 1: Hardcoded Path 扫描</name>
  <files>skills/**/*.md, scripts/*.py</files>
  <action>
    扫描以下文件中的硬编码路径：
    - skills/revolve-setup/SKILL.md
    - skills/research-pipeline/SKILL.md
    - skills/evolve-claude-md/SKILL.md
    - skills/yt-search/SKILL.md
    - scripts/sync_conversations.py

    搜索模式：
    - ~/Knowledge Base（用户特定路径）
    - /Users/（绝对用户路径）
    - hardcoded email 或 username

    对每个发现的 hardcoded 路径：修改为从 config.md 读取或使用 placeholder。
  </action>
  <verify>grep -r "Knowledge Base" skills/ scripts/ || echo "No hardcoded paths found"</verify>
  <done>扫描完成，所有 skill 和脚本中无硬编码用户路径</done>
</task>

<task type="auto">
  <name>Task 2: 端到端飞轮测试</name>
  <files>无文件修改</files>
  <action>
    执行完整飞轮测试流程验证：

    1. 配置测试
       - config.md.example 格式有效
       - docs/config-contract.md 中的所有字段在 config.md.example 中均有对应

    2. Skill 完整性检查
       - 4 个 SKILL.md 文件均存在
       - 每个 SKILL.md 包含 Trigger 和 Instructions 章节
       - plugin.json 的 skills 列表中 4 个路径均存在

    3. 脚本语法检查
       - python3 -m py_compile scripts/sync_conversations.py

    4. 模板完整性
       - templates/research-note.md 包含必需 frontmatter 字段
       - templates/research-index.md 包含 dataview 代码块

    5. 文档完整性
       - README.md 和 README_CN.md 均存在且非空
       - docs/flywheel.md 存在

    报告每项检查的通过/失败状态。
  </action>
  <verify>所有检查项均通过</verify>
  <done>端到端检查全部通过</done>
</task>

<task type="auto">
  <name>Task 3: 确认 plugin.json version</name>
  <files>.claude-plugin/plugin.json</files>
  <action>
    读取 .claude-plugin/plugin.json，确认：
    - version 字段为 "0.1.0"
    - skills 数组包含 4 个正确路径
    - 如有遗漏或错误，更新 plugin.json
  </action>
  <verify>cat .claude-plugin/plugin.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['version']=='0.1.0'"</verify>
  <done>plugin.json version=0.1.0，skills 列表完整</done>
</task>

<task type="auto">
  <name>Task 4: 完善 CHANGELOG.md</name>
  <files>CHANGELOG.md</files>
  <action>
    更新 CHANGELOG.md 的 [0.1.0] 条目，确保涵盖所有实际交付的内容：
    - 4 个 skills（revolve-setup, research-pipeline, evolve-claude-md, yt-search）
    - sync_conversations.py（4 provider 支持）
    - com.revolve.sync.plist（launchd 自动同步）
    - Obsidian templates（research-note, research-index）
    - 双语文档（README.md, README_CN.md）
    - docs/flywheel.md
    - CONTRIBUTING.md
    - config.md 配置系统
  </action>
  <verify>grep "0.1.0" CHANGELOG.md</verify>
  <done>CHANGELOG.md [0.1.0] 条目完整</done>
</task>

<task type="auto">
  <name>Task 5: 打 v0.1.0 tag</name>
  <files>无文件修改</files>
  <action>
    确认所有文件已提交到 git，然后打 tag：

    ```bash
    git add -A
    git commit -m "feat: initial release v0.1.0"
    git tag v0.1.0
    ```

    如果用户希望推送到 remote：
    ```bash
    git push origin main --tags
    ```

    询问用户是否现在推送到 remote。
  </action>
  <verify>git tag | grep v0.1.0</verify>
  <done>v0.1.0 tag 已创建</done>
</task>
</tasks>
